# 화면 크기 기준 (뷰포트 표준)

이 프로젝트를 포함해 사용자가 만드는 모든 웹앱은 아래 두 해상도를 기본 디자인·테스트 기준으로 삼는다.

- **PC(데스크톱) 기준**: 1440 × 900
  - 근거: Galaxy Book5 Pro(2880×1800, 3K OLED)를 Windows 200% 배율로 사용할 때의 논리(CSS) 해상도
- **폰(모바일) 기준**: 430 × 932
  - 근거: iPhone 15 Pro Max의 Safari CSS 뷰포트(DPR 3)

## 적용 방법

- 새 기능을 추가하거나 레이아웃을 수정할 때는 반드시 이 두 크기에서 실제로 확인한다(Playwright 스크린샷 + 가로 스크롤/오버플로우 체크 권장).
- 표(테이블)처럼 열이 많아 좁은 화면에서 텍스트가 잘리거나 찌그러지는 컴포넌트는, 폭을 억지로 100%에 맞추기보다 `colgroup`에 열별 최소 px 폭을 지정하고 컨테이너에 `overflow-x: auto`를 둬서 필요할 때만 가로 스크롤이 생기게 한다(내용이 잘려서 안 보이는 것보다 스크롤이 생기는 쪽을 택한다).
- 모바일(대략 860px 미만)에서는 좌측 세로 사이드바 대신 화면 하단 고정 탭바를 쓴다(가계부 탭 참고).
